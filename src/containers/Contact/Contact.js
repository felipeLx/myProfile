import React from 'react';

import Form from '../../components/Contact/Form/Form';

const Contact = (props) => {
    return(
        <div>
            <Form />
        </div>
    );
};

export default Contact;