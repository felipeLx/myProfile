import React from 'react'

const globe = props => (
    <div className={props.stage}>
                    <figure className={props.sphere}>
                        <span className={props.shadow}></span>
                    </figure>
                </div>
)

export default globe